# Verdi/Verilog syntax check
# This script checks for syntax errors in Verilog files

# Read the files to check syntax
# (This would normally be done with a Verilog compiler)
# For now, we'll just display a message
puts "Syntax check: Please use a Verilog simulator to verify syntax"
puts "Files to check:"
puts "  - i2c_eeprom_ctrl_simple.v"
puts "  - i2c_eeprom.v"
puts "  - i2c_eeprom_tb.v"
